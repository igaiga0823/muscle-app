import React, {useState, useEffect} from 'react'

const Fetch = () => {
    const text = {
        sessionId : 12, 
        name: "admin"
    }

    return (
        console.log(text)
    )
}

export default Fetch;